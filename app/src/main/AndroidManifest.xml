<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bangkit.waste">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Waste">
        <activity
            android:name=".IngredientsActivity"
            android:label="@string/ingredients" />
        <activity
            android:name=".ui.UkmActivity"
            android:label="@string/ukm" />
        <activity
            android:name=".ui.StepActivity"
            android:label="@string/step" />
        <activity android:name=".ui.ProductActivity" />
        <activity android:name=".ui.camera.ProductsActivity" />
        <activity android:name=".ui.camera.CategoriesActivity" />
        <activity
            android:name=".ui.home.HowToActivity"
            android:label="@string/how_to" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>